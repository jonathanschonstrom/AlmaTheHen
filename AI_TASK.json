{
  "protocol_version": "2.1",
  "task_id": "e0-sandbox-startup-finalize-001",
  "status": "ready",
  "parent_task": "e0-persistence-tests-001",
  "follows": "e0-sandbox-startup-006",
  "objective": "Record and publish the already validated sandbox-startup harness result. Do not perform another Godot run and do not change the validated harness logic.",
  "current_step_only": true,
  "required_working_directory": "C:\\AlmaTheHen",
  "expected_local_starting_head": "6c67818",
  "allowed_tracked_files": [
    "tests/test_persistence_migration.gd",
    "AI_RESULT.json"
  ],
  "preserve_existing_work": true,
  "validated_evidence": {
    "source_slice": "e0-sandbox-startup-006",
    "status": "success",
    "git_diff_check": "pass",
    "validation_command": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\tools\\bionic_harness_probe.ps1",
    "validation_result": "pass",
    "exit_code": 0,
    "wrapper_marker": "[WRAPPER] child_exit=0",
    "blockers": []
  },
  "do_not_do": [
    "Do not run Godot again in this slice.",
    "Do not modify tests/test_persistence_migration.gd after preflight.",
    "Do not modify scripts/persistence.gd or any implementation file.",
    "Do not run migration scenarios 1-14.",
    "Do not modify project.godot or VERSION.txt.",
    "Do not touch neural or behavior files.",
    "Do not merge or rebase main.",
    "Do not add tools/ runtime files to Git."
  ],
  "preflight": [
    "Confirm Get-Location resolves to C:\\AlmaTheHen.",
    "Confirm current branch is bionic/persistent-individual.",
    "Confirm current local HEAD is 6c67818 or an ancestor of origin/bionic/persistent-individual.",
    "Confirm tests/test_persistence_migration.gd exists as local preserved work.",
    "Record SHA256 of tests/test_persistence_migration.gd before synchronization.",
    "Confirm no unexpected tracked modifications other than AI_RESULT.json if present. If unexpected tracked changes exist, STOP and report BLOCKED."
  ],
  "actions": [
    "Run git fetch origin.",
    "Fast-forward only to origin/bionic/persistent-individual. Do not merge or rebase. If fast-forward is impossible, STOP and report BLOCKED.",
    "Verify the SHA256 of tests/test_persistence_migration.gd is unchanged after the fast-forward. If changed or missing, STOP and report BLOCKED.",
    "Write AI_RESULT.json for task e0-sandbox-startup-006 with protocol_version 2.0, status success, godot_runs_used 1 for slice 006, changed_files containing tests/test_persistence_migration.gd, validation_results recording git diff --check pass and the PowerShell harness probe pass with exit code 0 and [WRAPPER] child_exit=0, blockers as an empty array, and a concise summary that isolated sandbox startup is validated.",
    "Run git diff --check.",
    "Stage only tests/test_persistence_migration.gd and AI_RESULT.json.",
    "Commit with message: Validate isolated E0 sandbox startup harness",
    "Push branch bionic/persistent-individual to origin."
  ],
  "acceptance": [
    "No Godot run occurs.",
    "Validated tests/test_persistence_migration.gd content is preserved byte-for-byte across branch synchronization.",
    "AI_RESULT.json records the successful e0-sandbox-startup-006 result.",
    "git diff --check passes before commit.",
    "Only tests/test_persistence_migration.gd and AI_RESULT.json are committed by this slice.",
    "Push to origin/bionic/persistent-individual succeeds.",
    "Final report includes the pushed commit SHA and git status."
  ],
  "on_failure": "STOP and report BLOCKED with the exact failed step. Do not improvise synchronization, recovery, or additional validation runs."
}
