{
  "protocol_version": "1.3",
  "task_id": "e0-sandbox-startup-001",
  "status": "ready",
  "parent_task": "e0-persistence-tests-001",
  "objective": "Repair and validate only the isolated Godot sandbox/child-process startup path. Preserve the existing persistence test work. Do not validate migration scenarios yet. The slice is complete when the outer harness can create the sandbox outside the git worktree, launch a child Godot process, have the child print its OS.get_user_data_dir() and globalized user://alma/individual.json, prove that path is distinct from the real BirdAI/Alma user-data path, exit with code 0, and clean up the sandbox.",
  "current_step_only": true,
  "allowed_files": [
    "tests/test_persistence_migration.gd",
    "AI_RESULT.json"
  ],
  "preserve_existing_work": true,
  "do_not_do": [
    "Do not modify scripts/persistence.gd or any implementation file.",
    "Do not implement, debug, or run migration scenarios 1-14 in this slice.",
    "Do not rewrite the test suite from scratch.",
    "Do not change tracked project.godot.",
    "Do not commit or push until the slice validation passes."
  ],
  "runtime": {
    "godot_console": "C:\\BirdAI\\tools\\Godot_console.exe",
    "sandbox_must_be_outside_git_worktree": true,
    "sandbox_project_must_use_unique_user_data_namespace": true,
    "real_alma_user_data_must_not_be_written": true
  },
  "required_harness_output": [
    "[HARNESS] sandbox=<path>",
    "[CHILD] user_data_dir=<path>",
    "[CHILD] alma_save=<path>",
    "[ISOLATION] PASS",
    "[CLEANUP] PASS",
    "[HARNESS] PASS"
  ],
  "execution_rule": {
    "maximum_godot_runs": 2,
    "first_failed_run": "Inspect and report the exact stdout/stderr and identify one concrete harness defect. One targeted harness correction is permitted.",
    "second_failed_run": "STOP. Mark BLOCKED. Do not edit or run again.",
    "timeout_is_failure": true,
    "no_equivalent_retry_variants": true
  },
  "validation_commands": [
    "git diff --check",
    "C:\\BirdAI\\tools\\Godot_console.exe --headless --path . --script res://tests/test_persistence_migration.gd -- --harness-only"
  ],
  "acceptance": [
    "Child Godot process exits 0.",
    "No persistence migration scenario is executed.",
    "Sandbox user-data path is printed and proven distinct from the real BirdAI/Alma user-data path before any persistence code can write.",
    "Sandbox cleanup succeeds.",
    "git diff --check passes."
  ],
  "result_requirements": {
    "required_fields": [
      "protocol_version",
      "task_id",
      "status",
      "godot_runs_used",
      "changed_files",
      "validation_results",
      "isolation_evidence",
      "git_diff_check",
      "git_status",
      "summary",
      "blockers",
      "commit_sha"
    ],
    "status_values": [
      "success",
      "blocked"
    ]
  }
}
