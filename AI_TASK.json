{
  "protocol_version": "1.6",
  "task_id": "e0-godot-wrapper-launch-001",
  "status": "ready",
  "parent_task": "e0-persistence-tests-001",
  "supersedes": "e0-sandbox-startup-003",
  "resume_reason": "Direct execution of .\\tools\\Godot_console.exe is denied by the Bionic shell even though the file exists, is readable, and the same executable was manually run from PowerShell with version 4.7.2.stable.official.ed1daf0bf. This slice isolates the Bionic top-level execution boundary before returning to the Godot harness.",
  "objective": "Prove whether Bionic can launch the workspace-local Godot runtime as a child of PowerShell. Do not run the persistence harness or migration scenarios.",
  "current_step_only": true,
  "allowed_tracked_files": [
    "AI_RESULT.json"
  ],
  "allowed_untracked_runtime_file": "tools/bionic_godot_probe.ps1",
  "preserve_existing_work": true,
  "do_not_do": [
    "Do not modify tests/test_persistence_migration.gd.",
    "Do not modify scripts/persistence.gd or any implementation file.",
    "Do not run migration scenarios 1-14.",
    "Do not run the sandbox harness in this slice.",
    "Do not request broader filesystem access.",
    "Do not use C:\\BirdAI\\tools.",
    "Do not change Windows execution policy persistently.",
    "Do not retry equivalent quoting/path variants."
  ],
  "runtime": {
    "godot_console": "C:\\AlmaTheHen\\tools\\Godot_console.exe",
    "godot_version_expected": "4.7.2.stable.official.ed1daf0bf",
    "probe_script": "C:\\AlmaTheHen\\tools\\bionic_godot_probe.ps1",
    "deadline_seconds": 20
  },
  "probe_script_requirements": [
    "Create only the untracked ignored tools/bionic_godot_probe.ps1 file.",
    "The script must invoke $PSScriptRoot\\Godot_console.exe --version.",
    "The script must print the child exit code and propagate that exit code as its own process exit code.",
    "Do not add retries or fallback paths."
  ],
  "validation_commands": [
    "git diff --check",
    "powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\\tools\\bionic_godot_probe.ps1"
  ],
  "execution_rule": {
    "maximum_probe_runs": 1,
    "on_failure": "STOP and report BLOCKED with exact stdout/stderr/exit code. Do not try another shell, path, quoting form, or direct executable invocation.",
    "timeout_is_failure": true
  },
  "acceptance": [
    "PowerShell probe launches workspace-local Godot_console.exe.",
    "Godot prints version 4.7.2.stable.official.ed1daf0bf.",
    "Probe reports child exit code 0 and parent PowerShell exits 0 within 20 seconds.",
    "No tracked file changes occur except AI_RESULT.json if written.",
    "No persistence or sandbox test executes."
  ],
  "result_requirements": {
    "required_fields": [
      "protocol_version",
      "task_id",
      "status",
      "probe_runs_used",
      "stdout",
      "stderr",
      "exit_code",
      "elapsed_seconds",
      "git_diff_check",
      "git_status",
      "summary",
      "blockers"
    ],
    "status_values": [
      "success",
      "blocked"
    ]
  }
}
